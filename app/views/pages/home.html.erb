<center>
    <div class="description">
        <h1>The Price is right Home</h1>
        <p class="winorlose"><%= flash[:notice] %> </p>
        <p>Score: <%= score %></p>
        <p>We got a  <%= @item.title %></p>
        <p> <%= image_tag @item.picture %></p>
        <p>Description: <%= @item.description %></p>

        <p>How much is this worth?</p>
    </div>

    <div class="bottom-cards">
        <% @num = 0 %>
        <%  @item.choice.each do |c|  %>
            <% @num += 1 %>
            <a href="javascript:closeLogo('<%="/pages/result?utf8=✓&choice=#{c}&item=#{@item.id}" %>');"><span class="card">$<%= c %></span></a> <%= " | " if @num < @item.choice.size %>
        <% end %>
    </div>

</center>

<audio src="assets/crowdapplause1.mp3" preload>
 audio not supported
</audio>

<audio src="assets/crowdapplause1.mp3" preload>
 audio not supported
</audio>

